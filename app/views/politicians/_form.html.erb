<%= form_for(politician) do |f| %>
  <% if politician.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(politician.errors.count, "error") %> prohibited this politician from being saved:</h2>

      <ul>
      <% politician.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person %>
    <%= f.collection_select :person_id, Person.all, :id, :name %>
  </div>

  <div class="field">
    <%= f.label :party %>
    <%= f.collection_select :party_id, Party.all, :id, :abbreviation %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
